<h1>
    Course page that will list teams. 
</h1>

<p>
    Access all teams with @course.teams
</p>

test
<% @course.teams.each do |team| %>
    <p>
        <%= team.name %>
        Number of requests: <%= team.requests.length%>
        <%= button_to 'Request to Join', request_join_team_path(params[:id], team_id: team.id) %>
    </p>
<% end %>